setup()

// Green plays first
document.querySelector('.controller[data-player="green"]').dataset.active = true

// const cells = get_cells()
// generate_cells(cells, { green: [ELEMENTS.AIR], blue: [ELEMENTS.FIRE] })
// debug_display_variants(ELEMENTS.AIR)
